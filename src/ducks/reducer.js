// import axios from 'axios'

const initialState = {
    loading: false,
    name: '',
    address: '',
    city: '',
    state: '',
    zip: ''

}

export default function reducer(state = initialState, action){
    switch (action.type){
        default: 
        return state
    }
}